services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: tanaad
      POSTGRES_PASSWORD: tanaad
      POSTGRES_DB: tanaad
    ports:
      - "5432:5432"
    volumes:
      - tanaad_pgdata:/var/lib/postgresql/data

volumes:
  tanaad_pgdata:
